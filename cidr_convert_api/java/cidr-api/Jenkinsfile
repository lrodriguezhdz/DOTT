pipeline {
    agent any
       tools { 
        maven 'Maven' 
        jdk 'jdk8' 
    }
    stages {
        stage('Build 2') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}