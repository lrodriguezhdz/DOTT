pipeline {
    agent any
       tools { 
        maven 'Maven' 
    }
    stages {
        stage('Build 2') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}