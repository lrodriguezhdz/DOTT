pipeline {
   agent any
    stages {
        stage('Build') { 
            steps {
                sh 'pwd'
                sh 'cd cidr_convert_api'
                sh 'pwd'
                sh 'cd java'
                sh 'pwd'
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}