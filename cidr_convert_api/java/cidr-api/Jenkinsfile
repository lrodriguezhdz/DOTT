pipeline {
    agent any
       tools { 
        maven 'Maven' 
    }
    stages {
        stage('Build ') { 
            steps {
                sh 'pwd'
                sh 'mvn -f cidr_convert_api/java/cidr-api/pom.xml clean install -e'
                sh 'mvn -B cidr_convert_api/java/cidr-api/pom.xml -DskipTests clean package' 
            }
        }
    }
}